import torch
from transformers import AutoModel, AutoTokenizer

# tokenizer = AutoTokenizer.from_pretrained("vinai/phobert-base", use_fast=False)

# print(tokenizer.encode("Bức tranh thật đẹp"))

a = ['a', 'b']
if 'a' in a:
    print("cc")